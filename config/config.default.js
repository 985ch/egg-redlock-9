'use strict';

/**
 * egg-redlock-9 default config
 */
exports.redlock9 = {
  // clients: [ 'redis1' ],  // redis clients
  // client: 'redis1', // redis client,if redis.clients is undefined, both client and client can be omitted
  // logger: app => e => app.logger.info(e.message), // logger when lock failed, can be false
  // options: {
  //   driftFactor: 0.01, // time in ms
  //   retryCount: 5,
  //   retryDelay: 20, // time in ms
  //   retryJitter: 10, // time in ms
  // },
};
